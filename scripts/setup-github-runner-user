#!/usr/bin/env bash
#
# setup-github-runner-user — provision the dedicated `github-runner` system user.
#
#   * user github-runner, home /home/github-runner, shell /bin/bash, NO sudo/wheel
#   * purpose: run the self-hosted GitHub Actions runner and its rootless Docker
#     in isolation from your primary account. Workflow code executes as this
#     user, so it deliberately has no root.
#
# Run as root:  sudo scripts/setup-github-runner-user  [--dry-run]
# Idempotent; safe to re-run. Next: setup-github-runner-docker, then setup-github-runner.

set -euo pipefail

USER_NAME="github-runner"
USER_HOME="/home/github-runner"
USER_SHELL="/bin/bash"
DRY_RUN=false

usage() { awk 'NR==1{next} /^#/{sub(/^# ?/,"");print;next} {exit}' "$0"; }
for a in "$@"; do case "$a" in
  --dry-run) DRY_RUN=true ;;
  -h|--help) usage; exit 0 ;;
  *) echo "unknown argument: $a" >&2; usage >&2; exit 1 ;;
esac; done

log()  { printf '\033[1;36m==>\033[0m %s\n' "$*"; }
warn() { printf '\033[1;33m[!]\033[0m %s\n' "$*" >&2; }
die()  { printf '\033[1;31m[x]\033[0m %s\n' "$*" >&2; exit 1; }
run()  { if $DRY_RUN; then printf '    + %s\n' "$*"; else "$@"; fi; }

[ "$(id -u)" -eq 0 ] || die "must run as root (use sudo)"

if getent passwd "$USER_NAME" >/dev/null; then
  log "user $USER_NAME already exists — skipping useradd"
else
  log "creating user $USER_NAME (home=$USER_HOME, shell=$USER_SHELL, no sudo)"
  run useradd -m -d "$USER_HOME" -s "$USER_SHELL" "$USER_NAME"
fi

# Guard rail: the runner user must never have root.
if getent passwd "$USER_NAME" >/dev/null \
   && id -nG "$USER_NAME" | tr ' ' '\n' | grep -qxE 'wheel|sudo|docker'; then
  die "$USER_NAME is in wheel/sudo/docker — those are root-equivalent; remove it
      (e.g. gpasswd -d $USER_NAME docker). The runner user must have no root."
fi
$DRY_RUN || log "confirmed no root groups: $(id -nG "$USER_NAME" 2>/dev/null || echo '(pending)')"

log "done. Next: sudo scripts/setup-github-runner-docker"
