FLAKY-TEST LANE — spawn a singleton athena-admiral (context injected by flaky-ticket-poll.sh)

{{TICKET_LIST}}

There is queued flaky-test work assigned to {{OWNER_NAME}} and (per the marker
file) no athena-admiral is currently draining it. Spawn ONE athena-admiral subagent
(Agent tool, subagent_type "athena-admiral") to drain the entire flaky-test scope,
then STOP — do not do the work yourself.

Before you spawn it, create the running-marker so the next session's poll stays
quiet while this athena-admiral drains:

    touch ~/.claude/flaky-coordinator.lock

Give the athena-admiral EXACTLY this brief (it must never have to ask a clarifying
question — every foundational input is supplied here):

--- BEGIN ATHENA-ADMIRAL BRIEF ---
You are draining the walt_ui flaky-test lane. Work the ENTIRE in-scope set,
one ticket fully to merged before starting the next.

SCOPE (Notion "Tickets" database, reached via the notion-work MCP connector):
  - Database: "Tickets", id f00eab4f-26e1-4a97-8a2b-fd6a4a15323e
  - Filter (this is the complete queue definition — flaky + owner + queued):
      Labels (multi_select)  contains  "flaky-tests"
      AND Assignee (people)  contains  {{OWNER_NAME}}  (notion person id {{OWNER_ID}})
      AND Status (status)    is one of {"Todo", "Backlog"}
  - Re-run this exact query yourself each time you finish a ticket; new flaky
    tickets that land while you run belong to THIS run — keep draining until the
    query returns empty.

HOW "BLOCKED" IS REPRESENTED:
  - The `Blocked By` relation property. A ticket is blocked iff its `Blocked By`
    relation is non-empty. To mark a ticket blocked on a newly discovered
    dependency, set its `Blocked By` relation to the dependency ticket AND move
    its Status to "Needs Attention".

STATUS VALUES (use these exact existing options — do not create new ones):
  - Unblocked, work about to start        -> "In Progress"
  - Blocked on a discovered dependency    -> "Needs Attention"  (+ Blocked By set)
  - Verification passing, MR opened       -> "In Review"        (athena-captain sets this)
  - Stuck                                 -> "Needs Attention"
  - Merged and shipping                   -> "Ready for Release"
  - Fully done                            -> "Done"

CONCURRENCY:
  - MAX 1 athena-captain at a time. Never run two concurrently in this lane —
    drain strictly sequentially: dispatch one athena-captain, land its MR
    (auto-merge per your default), then dispatch the next.

MERGING / DEPLOY:
  - Auto-merge is your default — merge each MR yourself once it clears the full
    bar (local gate green, CI green on the head, both review bots actually ran
    with findings addressed and threads replied). Follow your normal
    Auto-Deploy / batch-and-watch rules.

When the scope query returns empty and every ticket you touched is merged,
remove the marker so future sessions can dispatch again:

    rm -f ~/.claude/flaky-coordinator.lock

Then produce your normal final report.
--- END ATHENA-ADMIRAL BRIEF ---

If the athena-admiral terminates or you abort this run without it clearing the
marker, delete ~/.claude/flaky-coordinator.lock yourself so the lane is not
wedged shut (the poll also self-heals a marker older than 12h).
