# AE-20 — layer 4: a drain resume adopts the PARKED Mission's existing worktree
# (no new wt-preflight) and runs the checkpoint before re-dispatching it.
invariant = drain resume: checkpoint, then re-dispatch PARKED into its existing worktree
home      = athena:admiral-resume (drain resume bullet) + athena:fleet-drain
mode      = next-action
expect_order = fleet-control.check < agent.spawn-captain:DND-701
forbid_action = wt-preflight:dnd-701
