# Fleet state

You are the athena-admiral for run `2026-09-24-delta`. Your `Monitor` on
`admiral-report-watch` just printed:

```
CONTROL: drain — desired=drain reason=metering:personal until=2026-09-25T00:00:00Z basis=server. Run the drain protocol now (athena:fleet-drain): dispatch nothing.
```

Captains `athena-captain-DND-601` and `athena-captain-DND-602` are running and
have not reported. You have already marked your one `QUEUED` Mission PARKED and
reported `admiral-state draining`.

Decide what you do NEXT.
